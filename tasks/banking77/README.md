# Banking77

Primary NanoClassify hill-climbing task.

The first implementation should adapt the confirmed reference container in
`/Users/joshuapurtell/GitHub/evals/containers/images/banking77`, pin its source
revision and task contract, and produce a reproducible exact-label baseline
before attempting optimization.
